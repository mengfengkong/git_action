# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!
        
      - name: Git relation
        run: |
          echo "${{ github.repository }}"
          echo "${{ github.head_ref }}"
          echo "${{ github.ref }}"
          echo "${{ github.event_path }}"
          echo "${{ github.ref_name }}"
          echo $(git rev-parse HEAD)
          echo $(git show -s --format=%an)

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          go run main.go
          # if [[ go run main.go ]]; then echo 1; else echo 0; fi
      - name: Correct
        run: |
          echo 222
      - name: Notify Test Failed
        if: ${{ failure() }}
        run: |
          curl -H "Content-Type: application/json" -d '{"msg_type":"interactive","card":{"config":{"wide_screen_mode":true},"i18n_elements":{"zh_cn":[{"tag":"div","text":{"tag":"lark_md","content":"**Github action:** ❌fail"}},{"tag":"div","text":{"tag":"lark_md","content":"**Branch:** ${{ github.ref_name }}"}},{"tag":"div","text":{"tag":"lark_md","content":"**Repository:** ${{ github.repository }}"}},{"tag":"div","text":{"tag":"lark_md","content":"**Author:** $(git show -s --format=%an)"}},{"tag":"div","text":{"tag":"lark_md","content":"**Address**: https://github.com/${{ github.repository }}"}}]}}}' ${{ secrets.FEISHU_WEBHOOK }}

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Test Deploy
        run: |
           echo xxx
          
